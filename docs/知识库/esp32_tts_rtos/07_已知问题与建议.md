# 已知问题与建议

## P0 严重（已修复，保留记录）
### P0-001: FreeRTOS 100Hz 节拍下 WAIT 超时漂移 10 倍（v0.5 修复）
- **文件**: main/rfid_task.c（rfid_process.c）
- **现象**: vTaskDelay(1)=10ms，"1ms 节拍"计数使 20ms 超时实际约 200ms、重发放弃约 400-600ms
- **修复**: 改真实时间差（now_ms() - wait_tick ≥ RFID_READ_TIMEOUT_MS），并修正"1ms 节拍"注释

### P0-002: NONE 态不轮询读卡号，询问模式模块永久失联（v0.5 修复）
- **文件**: main/rfid_task.c（rfid_process.c）
- **现象**: 若模块非自动上报模式，卡重新放入后无响应 → LED/播报/触发全失效
- **修复**: NONE 态每 200ms 发一次 Card_ReadCard 探测

### P0-003: 发送 0x7F 转义分支逻辑错误（v0.5 修复）
- **文件**: components/common/card_uart.c:UartSendCommand
- **现象**: 转义时 i+=1 会跳过输入字节并多读 1 字节（当前命令不含 0x7F 为死代码）
- **修复**: 输入/输出索引分离重写

### P0-004: FreeRTOS tick 100Hz 下 pdMS_TO_TICKS(1)=0 忙等/WDT 刷屏（2026-08 修复）
- **文件**: esp32/esp32_tts_rtos/sdkconfig.defaults（CONFIG_FREERTOS_HZ=1000）
- **现象**: tick=100Hz 时 pdMS_TO_TICKS(1)=0，vTaskDelay(0) 退化为忙等，IDLE 饿死引发 Task WDT 持续刷屏
- **修复**: tick 提到 1000Hz（vTaskDelay(1)=1ms）；注意 main/rfid_task.c 头部注释仍写"默认 100Hz"属过时注释，以 sdkconfig 为准

## P1 重要（已修复，保留记录）
### P1-001: 初始化 API 返回值全忽略（v0.5 修复）
- **修复**: uart/ledc/adc/gpio 初始化统一 ESP_ERROR_CHECK

### P1-002: 两版项目 18 个共享文件复制维护（v0.5 修复）
- **修复**: 抽取 components/common 组件 + run_tests.sh 漂移检查

### P1-003: TTS 上电提示音误配 <I>1（2026-08 修复，commit 1ba84fc）
- **文件**: components/common/tts.c:TTS_SetupDefaults
- **现象**: 原发 <I>1 以为是"关闭提示音"，实为选择 1 号音色
- **修复**: 试听后改发 <I>7（选定 7 号提示音）；<I> 指令本身启用断电保存

## 2026-08 功能变更记录（非缺陷，影响理解旧文档）
| 变更 | 说明 | 来源 |
|---|---|---|
| 自动停车网页化 | 电位器退役（ADC 无调用方），autostop_ms 网页可设 10~1000s | esp32/components/common/nvs_params.c、main/motor_task.c |
| LED 离场延时/读卡轮询间隔参数化 | 改读 g_params.led_on_ms / rfid_poll_ms | esp32/main/rfid_task.c |
| 参数全闭环 | 表单→JSON→GBK 转换→sanitize→NVS→重启生效 | esp32/components/common/{web_server,nvs_params,gbk_utf8}.c |
| 配网模式组件 | 连按3次RST / SoftAP / HTTP API | esp32/components/common/{config_mode,wifi_ap,web_server}.c |
| ws2812 RMT 驱动 | rmt_new_simple_encoder+callback；GRB 字节序 | esp32/components/common/ws2812.c |

## P2 遗留（低风险）
| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| P2-001 | RX 0x7F 反转义缺失 | GBK 不含 0x7F，实际不触发 | 解析时双字节合并 |
| P2-002 | speak_en=0 语义 | 仅取消强制播报 | 文档说明 |
| P2-003 | IDLE/RAMPUP 期触发延迟到 RUN 消费 | 上电 6s 内触发先跑后停 | RAMPUP 检查 pending |
| P2-004 | SLOW 窗口重启后二次进入 | 触发 26~31s 时重启后继续降速 | 加持久标志 |
| P2-005 | 绝对计时停车重启后立即 STOP | 设计既定 | 确认规则 |
| P2-006 | 中文路径依赖 newlib 配置 | 换 ASCII 路径可移除 CONFIG_LIBC_NEWLIB=y | 文档已注明 |
| P2-007 | params_t 结构变更使旧 NVS blob 失效（len 不符回落默认一次） | 升级后用户参数需重设 | 发布说明标注 |
| P2-008 | rfid_task.c 头部注释仍写"FreeRTOS 默认 100Hz" | 误导读者（实际 sdkconfig=1000Hz） | 择机改注释 |
| P2-009 | web_server.h 注释称 "POST /api/restart 保存并重启"，实现仅重启不保存（保存实际发生在 POST /api/params 内） | 理解偏差 | 改注释或改实现 |
| P2-010 | run_tests.sh 分段标签 [1/3]/[2/3]/[3/4]/[4/4] 序号混排 | 仅显示瑕疵 | 改为 [x/4] |
| P2-011 | utf8→GBK 反查 uni_to_gbk 为 8178 项线性扫描 | 仅配网保存时调用，运行时无影响 | 需要时可建反向索引 |
