# 已知问题与建议

## P0 严重（已修复，保留记录）
### P0-001: 开机波特率切换期 ORE 溢出致读卡永久失效
- **状态**: 已修复（v0.3）
- **文件**: Core/Src/usart.c（USART1_Init 2 区）、hardware/rfid_card/Card.c
- **现象**: 9600 期模块 0xAC 响应先于接收中断使能到达，RXNE 未被读走 → ORE → 读卡静默失效
- **修复**: ① 切换命令前先 HAL_UART_Receive_IT（乱码被解析器丢弃）② HAL_UART_ErrorCallback 自愈重装

### P0-002: 帧长度字段无上限校验 → ReceiveBuffer[32] 越界写
- **状态**: 已修复（v0.3）
- **文件**: hardware/rfid_card/Card.c:UartReceiveCommand
- **修复**: 长度 >31 直接丢弃整帧并复位状态机

### P0-003: 0xAC 响应误判为读块数据
- **状态**: 已修复（v0.3）
- **文件**: hardware/rfid_card/Card.c
- **修复**: 仅 0x91 产生 RESDATA 事件；0xAC/0x90 其他响应不产生事件

## P1 重要（已修复，保留记录）
### P1-001: 触发词状态数组硬编码 [4]，规则超 4 条越界
- **状态**: 已修复（v0.3）
- **修复**: config.h 新增 TRIGGER_RULES_MAX=8，数组按宏定义

### P1-002: 播报缓冲 16 字节全非零时越界读
- **状态**: 已修复（v0.3）
- **修复**: chinese_data 拷贝后强制 [15]=0 结尾

## P2 遗留（低风险，建议后续处理）
| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| P2-001 | RX 方向 0x7F 反转义缺失 | 卡数据含 0x7F 时帧错位（GBK 汉字不含 0x7F，实际不触发） | 解析时双字节合并 |
| P2-002 | speak_en=0 语义：仅取消强制播报，触发词仍会去重播报 | 配置 speak_en=0 时行为与"不播报"直觉不符 | 文档说明或改三态 |
| P2-003 | IDLE/RAMPUP 期触发延迟到 RUN 才消费 | 上电 6s 内刷触发词，车先跑起来才停 | RAMPUP 分支检查 pending_trigger |
| P2-004 | SLOW 窗口重启后可二次进入 | 触发在 26~31s 时重启后仍处窗口内继续降速 | 加"SLOW 已用"持久标志 |
| P2-005 | 绝对计时：停车重启后可能立即 STOP | 触发早时重启即停（设计既定）；2026-08 起 stop_time 固定为 g_params.autostop_ms，不再受电位器影响 | 确认规则后改相对计时 |
| P2-006 | UartSendCommand 转义依赖命令不含 0x7F | 当前命令字节固定不含，安全 | 加编译期断言 |
| P2-007 | card_res_flag 未声明 volatile（理论风险） | 实际安全（中断回调置位，任务轮询） | 已声明 volatile（v0.3 修复） |
| P2-008 | 重发"2 次后放弃"实际只重发 1 次（STM32） | 模块 2ms 内正常响应，无实际影响 | 计数语义统一 |

## 2026-08 新增问题与注意点（串口配置模式引入）
| 编号 | 问题 | 影响 | 建议 |
|---|---|---|---|
| P3-001 | printf 重定向在 TTS 语音口(huart2)，CLI/调试误用会把文本播进喇叭且占用语音口 | 调试体验差、可能干扰播报 | 调试与 CLI 应答一律 Dbg_Printf→USART3（param_cli.c 已遵守） |
| P3-002 | ISP 软跳前未拔 U13T 读卡模块时 Bootloader 应答被干扰 | ISP 烧录失败 | 操作规程：跳转前拔模块，烧完插回（isp_jump.h 注释） |
| P3-003 | params_t 结构变更会使旧 Flash blob 尺寸/CRC 不匹配 | 升级固件后参数回落默认一次（同 ESP32 NVS 教训） | 结构体只追加不改字段顺序；或加版本号 |
| P3-004 | ParamCli_Poll 为 RXNE 轮询无 FIFO，主循环长阻塞期字节可能溢出丢字 | RFID/Motor 长事务期间 CLI 粘包/丢字 | 行协议容错已较好；如恶化可改中断+环形缓冲 |
| P3-005 | DUMP 命令直接解引用 0x0800FC00 读 Flash | 正常；但 64KB 以下型号该地址越界 | 仅限 C8T6（64KB）使用 |
| P3-006 | **GET all 应答疑被截断**（2026-08 复核）：Dbg_Printf 格式化缓冲 128B，而 GET all 默认值紧凑 JSON 约 140 字节（含 "> " 前缀），vsnprintf 截至第 127 字节 | 尾部 "win_n"/"rule_n" 字段可能丢失；上位机 apply_params 依赖这两键做"删旧再建"，缺失会导致残留旧窗口/旧触发词（无板未验证，上板联调必测） | 拆两行应答或加大 dbg_buf 至 192B |
| P3-007 | CLI/GET all 秒值整除精度损失：<1000ms 的字段显示为 0（如默认 rfid_poll_ms=800 → "poll_s":0） | 上位机读回 poll_s=0 再下发 SET poll_s 0 → sanitize 钳到下限 100ms，与原值不符（无板未验证） | 秒字段改一位小数或毫秒直传 |
> **【2026-08-25 已修复批注】** 本节固件缺陷已全部修复并随代码提交：
> GET all 应答截断（拆两行输出，host.py 按行解析 kv 天然兼容）；秒值整除精度（<1s 字段输出一位小数，如 poll_s=0.8）；
> rfid_poll_ms 默认值疑义（恢复原语义 RFID_LED_POLL_MS=10ms——800ms 系参数化时误用播报延时宏 RFID_READ_DELAY_MS，
> sanitize 钳制下限同步 100→10ms，ESP32 共享层同批修正）；HELP 文案改为 GET all|win|rule 与实现对齐。
> 上文对应条目保留作历史记录。
